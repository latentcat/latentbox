import { genMetadata } from "@/lib/articles";
import { ArticleLayout } from "@/components/SimpleLayout";

export const article = {
  author: "Latent Cat",
  date: "2020-02-16",
  title: "Terms of Service",
  description: "Terms of Service of Latent Box",
};

export const metadata = genMetadata(article);

export default (props) => (
    <ArticleLayout title="Terms of Service" intro="" {...props}>
      To be written.
    </ArticleLayout>
);

TERMS OF SERVICE

To be written.
